/**
 * isString(value) check the value if string
 * @param { value } any type of value
 * @returns { boolean } returns true if String otherwise False
 * 
 */
export default function isString(value) {
    return typeof value === "string";
}