/*
    @isNull(value) Check a value if it is NULL
    @param { * } any type of value
    @returns { boolean } returns True value is null or false

*/

export default function isNull(value) {
    return value === null;
}
